# generate startgoal first using startgoalgenerate,
then prepare task ik first using taskik.py 
then build regrasp map using regriptpp_quick.py,
so the build regrasp will be very fast about 0.02s each task.(regripp.py version time is 1.6s)
in examples.the nxtglplot_experiment_quick will use regripp_quick.
nxtglplotreal is for real robot
pathplot is for plot path.(tbc:fixture table basket camera)
